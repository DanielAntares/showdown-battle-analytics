# Runtime dependencies, pinned to tested versions for reproducible installs
# (this is also what Streamlit Community Cloud installs to deploy the app).
requests==2.32.3
pandas==2.2.3
pyarrow==24.0.0        # parquet engine for the processed turn dataset
PyYAML==6.0.2          # config.yaml
tqdm==4.67.1           # progress bars for scrape/parse runs
numpy==2.2.0
scikit-learn==1.8.0    # baselines, calibration
lightgbm==4.6.0        # gradient-boosted win-prob model
matplotlib==3.10.9     # evaluation plots (reliability diagrams)
plotly==6.8.0          # interactive win-prob timeline in the app
streamlit==1.58.0      # demo app; uses width="stretch" + st.fragment
websocket-client==1.8.0  # live battle spectating over Showdown's WebSocket
