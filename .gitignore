# data is re-buildable from public APIs; keep the repo light.
# models/ IS committed (7 MB) so the deployed demo can serve predictions.
data/
!data/**/.gitkeep

.claude/settings.local.json

__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/
.env
.venv/
